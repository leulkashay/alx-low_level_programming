#include "main.h"

/**
* _abs - Calculated the absolute value of an integer.

* @n: The number to be computed.

* Return: "The absolute number of an integer.
*/
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
