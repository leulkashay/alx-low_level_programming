#include <stdio.h>
#include <stdlib.h>

/**
 * main - Entry point
 *
 * Return: Always 0(succes(
 */
int main(void)
{
	char alpha;

	for( alpha = 'a'; alpha <= 'z'; alpha++)
	{
		putchar(alpha);
	}
	putchar("\n");

	return (0);
}
