#include "main.h"

/**
* puts2 -  Prints half of the string
* @str: string pointer
*
* Return: Nothing
*/

void puts2(char *str)
{
	int i;

	for (i = 0; str[i] != '\0'; i++)
	{
		if (i %  == 0)
		{
			_putchar(str[i]);
		}
	}
	_putchar('\n');
}
